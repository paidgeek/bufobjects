static const uint32_t kSize = 0
{%- for field in definition.fields -%}
  {%- set camelCase = utils.toCamelCase(field.name) -%}
  {%- if (utils.getClassName(field.type.definition) == "EnumDefinition") -%}
     + {{ typeSizes[field.type.definition.type.toString()] }}
  {%- elseif (field.type.token.toString() == "IDENTIFIER") -%}
    + {{ trim(macros.getTypeNameForDefinition(field.type.definition)) }}.SIZE;
  {%- else -%}
    + {{ typeSizes[field.type.token.toString()] }}
  {%- endif -%}
{%- endfor -%}
;