namespaceName: "{{ lower(name) }}"
className: "{{ name }}"
interfaceName: "{{ name }}"
structName: "{{ name }}"
enumName: "{{ name }}"
classField: "{{ utils.toSnakeCase(name) }}_"
structField: "{{ utils.toSnakeCase(name) }}"
method: "{{ name }}"
parameter: "{{ utils.toSnakeCase(name) }}"
getter: "{{ utils.toSnakeCase(name) }}"
setter: "set_{{ utils.toSnakeCase(name) }}"
enumValue: "k{{ name }}"
constant: "k{{ name }}"
