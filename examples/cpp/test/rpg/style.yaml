namespaceName: "{{ lower(name) }}"
className: "{{ name }}"
interfaceName: "{{ name }}"
structName: "{{ name }}"
enumName: "{{ name }}"
classField: "{{ utils.toSnakeCase(name) }}_"
structField: "{{ utils.toSnakeCase(name) }}"
method: "{{ utils.toCamelCase(name) }}"
parameter: "{{ utils.toCamelCase(name) }}"
getter: "get{{ name }}"
setter: "set{{ name }}"
enumValue: "{{ utils.toUpperCase(name) }}"
constant: "k{{ name }}"
